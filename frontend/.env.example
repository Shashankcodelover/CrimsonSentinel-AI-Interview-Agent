# Optional. Leave unset to use the local mock at /api/interview.
# When the real backend is deployed, set this to that URL and restart `npm run dev`.
# NEXT_PUBLIC_INTERVIEW_API_URL=https://example.com/api/interview
